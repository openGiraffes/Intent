import { Vue, Component, Prop, Inject } from "vue-property-decorator";
@Component
export default class MyFragment extends Vue {


    onPageResult(requestCode: number, resultCode: number, data: any) {

    }
}